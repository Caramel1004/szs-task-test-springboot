package com.szs.task.configration;

import com.szs.task.domain.configration.JwtConfigProps;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest
public class ConfigTest {
//    private final JwtConfigProps jwtConfigProps;
//
//    @Autowired
//    public ConfigTest(JwtConfigProps jwtConfigProps) {
//        this.jwtConfigProps = jwtConfigProps;
//    }
//
//
//    @Test
//    @DisplayName("환경 변수 세팅 테스트")
//    void configTest() {
//        assertThat(jwtConfigProps.getSecretKey()).isEqualTo("l4(&:+*kc&uf$<0q5(9(n]cNricB'Yx(1l;YEsKE83l]lCOJrsg,YT\\LL4yH7G1K");
//        assertThat(jwtConfigProps.getPrefix()).isEqualTo("Bearer");
//        assertThat(jwtConfigProps.getHeader()).isEqualTo("Authorization");
//    }

}
